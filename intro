zeppelin 只支三种表单控件：输入框、下拉框、复选框，样式也不能通过参数修改，所以修改。
方法一：
修改源代码，添加多种控件
这种方法，需要修改的东西较多，也设计到后端的东西，如果修改，需要编译。

方法二：
通过灵活的方法，只修改前段代码，来达到自己的效果。
简单方便，及时不需要编译
前端由 angular 开发，修改的话简单

思路：
1 修改样式文件 /styles/vendor.xxxxx.js
2 修改对应的前端 html 文件。目前只是修改表单相关的，那么就是修改 p/app/notebook/paragraph/paragraph-parameterizedQueryForm.html。如果要修改别的页面元素，找到对应的页面html文件修改即可
3 修改 angular 的控制器文件

修改点：
1 支持下拉多选，多选支持全选、全不选逻辑（需要修改控制器的逻辑）
2 支持时间选择器
3 支持时间段的坐标选择
4 支持控制非管理员不能打开代码页面
5 折线图在最后显示一个数值
